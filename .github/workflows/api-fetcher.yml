name: Run news api fetcher

on: push
#  schedule:
#    - cron: "* * * * *"

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - name: Just an echo
      - run: echo 'hellow world'
        shell: bash